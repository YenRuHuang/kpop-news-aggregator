{
  "build": {
    "commands": [
      "npm install",
      "cd frontend && npm install",
      "cd backend && npm install",
      "cd frontend && npm run build"
    ]
  },
  "start": "cd backend && npm start",
  "env": {
    "NODE_ENV": "production",
    "PORT": "8080"
  }
}